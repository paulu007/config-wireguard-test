## Test Wireguard 
Python app change wireguard config 
Work on Linux

## Installation

```bash
sudo apt install wg-quick
```

## Usage

```bash
sudo python3 main.py --conf-dir ./conf
```
Will genrate health config (stealth) for work on Wiresock on windwos
